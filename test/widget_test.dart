// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:mygrapgview/main.dart';

void main() {
  // testWidgets('Add a relation to the tree', (WidgetTester tester) async {
  //   await tester.pumpWidget(LayerGraphPageFromJson());

  //   var fromTextInputFinder = find.byKey(const Key('from_text_input'));
  //   var toTextInputFinder = find.byKey(const Key('to_text_input'));
  //   var addButtonFinder = find.byKey(const Key('add_btn'));

  //   await tester.enterText(fromTextInputFinder, 'node1');
  //   await tester.pump();
  //   await tester.enterText(toTextInputFinder, 'node2');
  //   await tester.pump();
  //   await tester.tap(addButtonFinder);
  //   await tester.pump();

  //   expect(find.text('node1'), findsOneWidget);
  //   expect(find.text('node2'), findsOneWidget);
  // });
}
